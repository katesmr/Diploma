/users/create
/users/delete/<int:id>/
/users/  -------> get all users
/users/<int:id>
/users/update/<int:id>/

/sounds/<str:name>  -------> get sound by id (user's id will get from request)
/sounds/  -----> get all sounds (user's id will get from request)
/sounds/create/<str:name>/  ------>  create sound (user's id and sound will get from request)
/sounds/delete/<str:name/  ------>  remove sound (user's id will get from request)
/sounds/update/<str:name/

simple json
{
    'stream': ['path', 'path'],
    'name': '',
    'settings': {},
}
/projects/<str:name>  -------> get sound by id (user's id will get from request)
/projects/  -----> get all sounds (user's id will get from request)
/projects/create/<str:name>/  ------>  create sound (user's id and sound will get from request)
/projects/delete/<str:name/  ------>  remove sound (user's id will get from request)
/projects/update/<str:name/
