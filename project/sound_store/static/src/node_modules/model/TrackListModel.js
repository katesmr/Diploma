var ObservableList = require("./ObservableList");
var inherit = require("../utils/inherit");

module.exports = TrackListModel;

function TrackListModel(name){
    ObservableList.call(this);
    this.name = name;
}

inherit(TrackListModel, ObservableList);

TrackListModel.prototype.add = function(data){
    var track;
    if(data.type === "synth"){
        track = new TrackSynthesizer(i+1, key, data);
    } else if(data.type === "noise"){
        track = new TrackNoise(i+1, key, data);
    } else{
        // DEFAULT_JSON_SETTINGS

    }

    if (track){
        ObservableList.prototype.add.call(this, track);
    }
};
